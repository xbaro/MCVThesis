# Database configuration
DB_USER=mcv_theses
DB_PASSWORD=.mcv_theses.
DB_NAME=mcv_theses
DB_HOST=db
DB_DIALECT="postgres"

# SSL configuration
# SSL_PATH=/ssl
SSL_KEY=test.key
SSL_CRT=test.crt

# SMTP Credentials (Currently only GMAIL is accepted)
SMTP_USER=
SMTP_PASSWORD=

# Number of threads to start on the web docker (-1 => Number of cores)
NUM_THREADS=4

# JWT configuration
TOKEN_HEADER_KEY=JWT
JWT_SECRET_KEY=
MCV_WEB_CLIENT=
MCV_WEB_SECRET=
